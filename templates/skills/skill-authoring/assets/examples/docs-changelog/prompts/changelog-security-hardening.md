Create a short changelog entry for the latest installer security hardening update.

Requirements:

- Focus on user-visible behavior only.
- Mention that API keys are now expected under `~/.opencode/keys`.
- Mention that missing-key warnings are clearer.
- Keep the note concise and release-note style.
- Save the result under `ai-docs/changelogs/security-hardening.md`.
